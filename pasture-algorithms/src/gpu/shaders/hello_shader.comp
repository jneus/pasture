#version 450

struct PointBuffer {
    dvec4 position;
    u32 index;
};

layout(std430, binding=0) buffer PointBufferSsbo {
    PointBuffer pointBuffer[];
};
layout(binding=1) uniform PointBuffer query;

void main() {

    uint idx = gl_GlobalInvocationID.x;


}
